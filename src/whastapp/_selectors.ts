enum SELECTORS {
    LOGIN_CHECK = '.landing-title',
    LOGIN_QRCODE = 'canvas',
    SEARCH = '[contenteditable="true"]',
    FIRST_CHAT = '[aria-label="Search results."] > div',
    TEXT_INPUT = 'footer [contenteditable="true"]',
    INPUTS = '.message-in',
    MESSAGE_CONTENT = '.copyable-text',
}

export default SELECTORS
